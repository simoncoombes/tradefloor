# The learn prerender reads these byte-for-byte and CRLF doubles its
# extracted newlines, which broke every SCRIPT_FIXES anchor on a Windows
# checkout after a rebase. LF in the working tree everywhere, always.
* text=auto eol=lf
*.png -text
*.ico -text
*.whl -text
